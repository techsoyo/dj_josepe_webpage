"use client";

// This page serves as the entry for the "Inicio" option in the
// navigation bar.  It renders the video landing page that was
// previously built in the referencias/dj_josepe_gasp project.  The
// component is isolated and uses styled‑jsx to prevent its styles
// from leaking into the rest of the application.

import VideoSection from '../../components/VideoSection';

export default function InicioPage() {
  return <VideoSection />;
}